# Football Prediction App

## Requirements

- Node.js
- RapidAPI Key (Football Prediction API)

## Setup

1. Clone
2. Run `npm install`
3. Create `.env` with `RAPIDAPI_KEY=YOUR_KEY`
4. Run `npm run dev`
5. Visit `http://localhost:3000`
